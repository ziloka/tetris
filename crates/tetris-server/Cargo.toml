[package]
name = "tetris-server"
version = "0.1.0"
edition = "2021"

[lib]
name = "tetris_server"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
spacetimedb = "1.12.0"
tetris-core = { path = "../tetris-core" }
